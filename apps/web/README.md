# apps/web

Next.js frontend for KBO Lineup Lab.

See the root [`README.md`](../../README.md#frontend) `## Development` section for run, lint, and format commands. This project uses npm only (no yarn / pnpm / bun).
